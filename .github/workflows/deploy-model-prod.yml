name: Prod Model Deployment for yape-mlstacks-2
'on':
  workflow_dispatch:
    inputs:
      modelUri:
        type: string
        description: URI of model to deploy, in the format models:/<model-name>/<version-id>
        required: true
env:
  DATABRICKS_HOST: https://adb-2529239831454482.2.azuredatabricks.net
  NODE_TYPE_ID: Standard_D3_v2
jobs:
  prod:
    runs-on: ubuntu-latest
    environment: prod
    steps:
    - name: Checkout repo
      uses: actions/checkout@v3
    - name: Generate AAD Token
      run: ./.github/workflows/scripts/generate-aad-token.sh ${{ secrets.prodAzureSpTenantId
        }} ${{ secrets.prodAzureSpApplicationId }} ${{ secrets.prodAzureSpClientSecret
        }}
    - name: Deploy model
      env:
        MLFLOW_TRACKING_URI: databricks
      run: 'pip install mlflow

        python yape-mlstacks-2/deployment/model_deployment/deploy.py "${{ github.event.inputs.modelUri
        }}" "prod"

        '
